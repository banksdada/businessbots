@props(['label', 'value'])

<div class="bg-surface border border-border rounded-xl p-4">
    <div class="text-xs text-text-secondary font-medium">{{ $label }}</div>
    <div class="mt-1 text-2xl font-bold text-text-primary">{{ $value }}</div>
</div>
